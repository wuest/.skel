" Notes {
"   GVim is set up to present a similar interface as vim in a terminal.  Most
"   gui niceties are stripped away.
" }

" Basic {
    " Use a minimalistic interface
    set guioptions=ac

    " Give a larger interface by default
    set lines=55 columns=100
" }
